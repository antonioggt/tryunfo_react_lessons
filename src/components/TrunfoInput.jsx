import React from 'react';

class TrunfoInput extends React.Component {
  render() {
    const { value } = this.props

    return (
      <input type="checkbox" value={ value } onChange={} />
    );
  }
}

export default TrunfoInput;
